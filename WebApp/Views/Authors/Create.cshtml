@model WebApp.Models.Author.CreateAuthorModel

<div class="container">
    <form id="createAuthorForm" class="w-50 m-5 p-3 shadow mx-auto" method="post">
        <div class="mb-3">
            <label for="Name" class="form-label">Name:</label>
            <input type="text" class="form-control" name="Name" id="Name" asp-for="Name" />
        </div>
        <div class="mb-3">
            <button type="button" class="btn btn-primary w-100" id="saveButton">Save</button>
        </div>
    </form>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        $(function () {
            $('#saveButton').click(function () {
                var formData = {
                    Name: $('#Name').val()
                };

                $.ajax({
                    url: baseUrl + 'api/Authors/Add',
                    type: 'POST',
                    headers: {
                        'Authorization': 'Bearer @ViewData["Token"]'
                    },
                    contentType: 'application/json',
                    data: JSON.stringify(formData),
                    success: function (response) {
                        Swal.fire({
                            title: 'Success!',
                            text: 'Yazar başarıyla eklendi!',
                            icon: 'success',
                            confirmButtonText: 'OK'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                window.location.href = '@Url.Action("GetList", "Authors")';
                            }
                        });
                    },
                    error: function (xhr, status, error) {
                        var errorMessage;
                        if (xhr.status === 401) {
                            errorMessage = 'Yetkilendirme hatası! Lütfen giriş yapın.';
                        } else {
                            errorMessage = xhr.responseJSON ? xhr.responseJSON.message : 'Yazar eklenirken bir hata oluştu.';
                        }

                        Swal.fire({
                            title: 'Error!',
                            text: errorMessage,
                            icon: 'error',
                            confirmButtonText: 'OK'
                        });
                    }
                });
            });
        });
    </script>
}
